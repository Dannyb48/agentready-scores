# Replace with your GitHub org name
org: konflux-ci

# Repos to assess when using --from-file runner/repos.yaml
repos:
  - devlake
  - build-service
  - integration-service
  - release-service
  - mintmaker
  - build-definitions

# Repos to skip in all modes (--from-file and --org)
# exclude:
#   - archived-repo
#   - broken-repo
